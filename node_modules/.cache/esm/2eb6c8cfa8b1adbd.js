_a8b‍.x([["default",()=>_AbstractLinkedList]]);// this is primarily a helper class for single linked list and double linked list

               class _AbstractLinkedList{

    // validates if a function received a valid number paramter in respects to the lis
    _validateNumberParam(number){
        if(number < 0){
            throw new RangeError("Parameter can not be a negative number")
        }
        // value isn't a number
        if(typeof value !== "number"){
            throw new TypeError("Parameter must be a number")
        }
        // value wasnt passed
        if(value == null){
            throw new Error("Parameter can not be empty")
        }
        // value exceeds size
        if(value > this.size-1){
            throw new RangeError("value paramater exceeds list length")
        }
    }
        // debug tool
        printList(params){
            if(params){
                throw new Error("Function does not take parameters")
            }
    
            let node = this.head
    
            for(let i = 0;i<this.size;i++){
                console.log(`CURRENT NODE INDEX: ${i}`)
                console.log("CURRENT NODE VALUE",node.value)
                console.log("--------------------------------")
                node = node.next
            }
        }

}